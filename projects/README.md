# Projects

This repository contains multiple sub-projects. Each project is intended to be runnable and versioned independently.

## Current projects
- `text-vector-db/`: Build a persistent vector database from internal `.txt` documents (Python + Chroma).

